# This's a personal configuration. I use debian system.

# If you want to add this config to your system. just paste the shell below into ~/.bashrc
# if [ -f ~/.bash_aliases ]; then
#     . ~/.bash_aliases
# fi

# some ls aliases
alias ls='ls --color=auto'
alias ll='ls -lh'
alias la='ls -A'
alias l='ls -CF'

# some filesystem aliases
alias ..='cd ..'
alias ...='cd ../..'

# some filter aliases 
alias grep='grep --color=auto'

# some system info aliases

# some git aliases
alias gitp='git pull --rebase'
